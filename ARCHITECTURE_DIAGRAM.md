# Архітектура Adelante CRM - Візуальна схема

## 🏗️ Загальна архітектура системи

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Next.js)                       │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    Презентаційний шар                       │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │ │
│  │  │ Календар │ │ Клієнти  │ │  Персонал│ │ Послуги  │      │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │ │
│  │  │ Фінанси  │ │  Склад   │ │  Огляд   │ │Налашт-ня │      │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                   State Management (Zustand)                │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐          │ │
│  │  │ Auth    │ │Calendar │ │ Clients │ │  Staff  │          │ │
│  │  │ Store   │ │  Store  │ │  Store  │ │  Store  │          │ │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘          │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐          │ │
│  │  │Services │ │Finances │ │Inventory│ │ Notif.  │          │ │
│  │  │ Store   │ │  Store  │ │  Store  │ │  Store  │          │ │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                      API Client Layer                       │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  Axios Instance (interceptors, auth, error handling) │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐        │ │
│  │  │Auth│ │Cal.│ │Cli.│ │Sta.│ │Ser.│ │Fin.│ │Inv.│        │ │
│  │  │API │ │API │ │API │ │API │ │API │ │API │ │API │        │ │
│  │  └────┘ └────┘ └────┘ └────┘ └────┘ └────┘ └────┘        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    WebSocket Client                         │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  Real-time updates (appointments, notifications)     │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↕ HTTP/HTTPS + WebSocket
┌─────────────────────────────────────────────────────────────────┐
│                         BACKEND (Node.js)                        │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    API Gateway / Router                     │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  Express/Fastify + Middleware (CORS, Auth, Logging) │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                  Authentication Layer                       │ │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐             │ │
│  │  │ JWT Auth   │ │   Guards   │ │ Permissions│             │ │
│  │  └────────────┘ └────────────┘ └────────────┘             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    Business Logic Layer                     │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │ │
│  │  │  Auth    │ │Appoint-  │ │ Clients  │ │  Staff   │      │ │
│  │  │ Service  │ │  ments   │ │ Service  │ │ Service  │      │ │
│  │  │          │ │ Service  │ │          │ │          │      │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │ │
│  │  │ Services │ │ Finances │ │Inventory │ │ Overview │      │ │
│  │  │ Service  │ │ Service  │ │ Service  │ │ Service  │      │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐                   │ │
│  │  │ Settings │ │  Notif.  │ │ Reports  │                   │ │
│  │  │ Service  │ │ Service  │ │ Service  │                   │ │
│  │  └──────────┘ └──────────┘ └──────────┘                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    Data Access Layer (ORM)                  │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  Prisma ORM (Type-safe database access)             │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    External Services                        │ │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐  │ │
│  │  │ Email  │ │  SMS   │ │  File  │ │WebSocket│ │ Queue  │  │ │
│  │  │Service │ │Service │ │Storage │ │ Server  │ │(Bull)  │  │ │
│  │  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↕ SQL
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE (PostgreSQL)                       │
│                                                                  │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │
│  │ Users  │ │Clients │ │ Staff  │ │Services│ │Appoint-│       │
│  │        │ │        │ │        │ │        │ │ ments  │       │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘       │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │
│  │Finance │ │Products│ │ Reviews│ │ Changes│ │  Notif.│       │
│  │        │ │        │ │        │ │        │ │        │       │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                        CACHE (Redis)                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Session Storage, Refresh Tokens, Cached Data            │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Потік даних

### 1. Аутентифікація
```
User Input (Login Form)
    ↓
Frontend: useAuthStore.login()
    ↓
API Client: auth.login(email, password)
    ↓ HTTP POST /auth/login
Backend: AuthController.login()
    ↓
Backend: AuthService.validateCredentials()
    ↓
Database: SELECT * FROM users WHERE email = ?
    ↓
Backend: AuthService.generateTokens()
    ↓
Redis: SAVE refresh_token
    ↓ Response { token, refreshToken, user }
Frontend: Store token in localStorage
    ↓
Frontend: Update useAuthStore state
    ↓
Frontend: Redirect to /calendar
```

### 2. Створення запису
```
User Input (CreateAppointmentModal)
    ↓
Frontend: useCalendarStore.createAppointment(data)
    ↓
API Client: appointments.createAppointment(data)
    ↓ HTTP POST /appointments
Backend: AppointmentsController.create()
    ↓
Backend: AuthGuard (verify JWT)
    ↓
Backend: AppointmentsService.create()
    ↓
Backend: Validate data
    ↓
Backend: Check staff availability
    ↓
Database: INSERT INTO appointments
    ↓
Backend: NotificationsService.create()
    ↓
Database: INSERT INTO notifications
    ↓
Backend: EmailService.sendConfirmation()
    ↓
WebSocket: Broadcast 'appointment:created'
    ↓ Response { appointment }
Frontend: Update useCalendarStore
    ↓
Frontend: Update UI
    ↓
Frontend: Show success toast
    ↓
Frontend: Close modal
```

### 3. Real-time оновлення
```
Backend: Event occurs (new appointment)
    ↓
Backend: WebSocket Server
    ↓ Emit 'appointment:created'
Frontend: WebSocket Client receives event
    ↓
Frontend: useNotificationsStore.addNotification()
    ↓
Frontend: useCalendarStore.addAppointment()
    ↓
Frontend: UI updates automatically
    ↓
Frontend: Show notification badge
```

---

## 📦 Структура модулів

### Frontend модуль (приклад: Clients)
```
src/features/clients/
├── types.ts                 # TypeScript типи
├── constants.ts             # Константи
├── data/
│   └── mockClients.ts       # Mock дані (тимчасово)
├── components/
│   ├── ClientsLayout.tsx    # Головний layout
│   ├── ClientsHeader.tsx    # Заголовок з пошуком
│   ├── ClientsFiltersBar.tsx# Фільтри
│   ├── ClientsTable.tsx     # Таблиця
│   ├── ClientsTableHeader.tsx
│   ├── ClientRow.tsx        # Рядок таблиці
│   ├── ClientActionsMenu.tsx# Меню дій
│   └── ui/
│       └── ClientCard.tsx   # Картка клієнта
├── modals/
│   ├── AddClientModal.tsx
│   ├── EditClientModal.tsx
│   ├── ClientDetailsModal.tsx
│   ├── ImportExcelModal.tsx
│   └── ExportExcelModal.tsx
└── hooks/
    └── useClientFilters.ts  # Хук для фільтрів
```

### Backend модуль (приклад: Clients)
```
src/modules/clients/
├── clients.module.ts        # Module definition
├── clients.controller.ts    # HTTP endpoints
├── clients.service.ts       # Business logic
├── dto/
│   ├── create-client.dto.ts # Validation schemas
│   ├── update-client.dto.ts
│   └── client-filters.dto.ts
├── entities/
│   └── client.entity.ts     # Database entity
└── tests/
    ├── clients.controller.spec.ts
    └── clients.service.spec.ts
```

---

## 🔐 Безпека

### Frontend
```
┌─────────────────────────────────────────┐
│  1. User enters credentials             │
│  2. Send to backend (HTTPS)             │
│  3. Receive JWT token                   │
│  4. Store in localStorage               │
│  5. Add to every request (Authorization)│
│  6. Auto-refresh when expired           │
│  7. Clear on logout                     │
└─────────────────────────────────────────┘
```

### Backend
```
┌─────────────────────────────────────────┐
│  1. Receive request                     │
│  2. Extract JWT from header             │
│  3. Verify signature                    │
│  4. Check expiration                    │
│  5. Extract user ID and role            │
│  6. Check permissions                   │
│  7. Allow/Deny request                  │
└─────────────────────────────────────────┘
```

---

## 📊 Приклад повного циклу CRUD

### CREATE (Створення клієнта)
```
Frontend                          Backend                      Database
   │                                 │                            │
   │ 1. User fills form              │                            │
   │ 2. Click "Створити"             │                            │
   │                                 │                            │
   │ 3. Validate form                │                            │
   │ 4. useClientsStore.create()     │                            │
   │ 5. api.clients.create(data)     │                            │
   │                                 │                            │
   │────POST /clients───────────────▶│                            │
   │    { firstName, lastName, ... } │                            │
   │                                 │                            │
   │                                 │ 6. AuthGuard.verify()      │
   │                                 │ 7. Validate DTO            │
   │                                 │ 8. Check phone unique      │
   │                                 │                            │
   │                                 │────SELECT phone───────────▶│
   │                                 │◀───Result: not exists──────│
   │                                 │                            │
   │                                 │ 9. Create client           │
   │                                 │────INSERT INTO clients────▶│
   │                                 │◀───New client ID───────────│
   │                                 │                            │
   │                                 │ 10. Create loyalty card    │
   │                                 │────INSERT INTO loyalty────▶│
   │                                 │◀───Success─────────────────│
   │                                 │                            │
   │                                 │ 11. Create change log      │
   │                                 │────INSERT INTO changes────▶│
   │                                 │◀───Success─────────────────│
   │                                 │                            │
   │◀───Response: { client }─────────│                            │
   │                                 │                            │
   │ 12. Update store                │                            │
   │ 13. Update UI                   │                            │
   │ 14. Show toast "Успішно"        │                            │
   │ 15. Close modal                 │                            │
```

### READ (Отримання списку)
```
Frontend                          Backend                      Database
   │                                 │                            │
   │ 1. Page loads                   │                            │
   │ 2. useClientsStore.fetch()      │                            │
   │ 3. api.clients.getClients()     │                            │
   │                                 │                            │
   │────GET /clients?page=1&limit=20─▶│                            │
   │                                 │                            │
   │                                 │ 4. AuthGuard.verify()      │
   │                                 │ 5. Parse query params      │
   │                                 │ 6. Build SQL query         │
   │                                 │                            │
   │                                 │────SELECT * FROM clients──▶│
   │                                 │    LIMIT 20 OFFSET 0       │
   │                                 │◀───20 clients──────────────│
   │                                 │                            │
   │                                 │────SELECT COUNT(*)────────▶│
   │                                 │◀───Total: 150──────────────│
   │                                 │                            │
   │                                 │ 7. Calculate segments      │
   │                                 │ 8. Format response         │
   │                                 │                            │
   │◀───Response: {                  │                            │
   │      data: [...],               │                            │
   │      pagination: {...}          │                            │
   │    }────────────────────────────│                            │
   │                                 │                            │
   │ 9. Update store                 │                            │
   │ 10. Render table                │                            │
```

### UPDATE (Оновлення клієнта)
```
Frontend                          Backend                      Database
   │                                 │                            │
   │ 1. User edits client            │                            │
   │ 2. Click "Зберегти"             │                            │
   │ 3. useClientsStore.update()     │                            │
   │                                 │                            │
   │────PUT /clients/:id─────────────▶│                            │
   │    { firstName, lastName, ... } │                            │
   │                                 │                            │
   │                                 │ 4. AuthGuard.verify()      │
   │                                 │ 5. Check permissions       │
   │                                 │ 6. Validate DTO            │
   │                                 │                            │
   │                                 │────SELECT * FROM clients──▶│
   │                                 │    WHERE id = ?            │
   │                                 │◀───Old client data─────────│
   │                                 │                            │
   │                                 │ 7. Update client           │
   │                                 │────UPDATE clients─────────▶│
   │                                 │    SET ... WHERE id = ?    │
   │                                 │◀───Success─────────────────│
   │                                 │                            │
   │                                 │ 8. Log change              │
   │                                 │────INSERT INTO changes────▶│
   │                                 │    (old_value, new_value)  │
   │                                 │◀───Success─────────────────│
   │                                 │                            │
   │◀───Response: { client }─────────│                            │
   │                                 │                            │
   │ 9. Update store                 │                            │
   │ 10. Update UI                   │                            │
   │ 11. Show toast                  │                            │
```

### DELETE (Видалення клієнта)
```
Frontend                          Backend                      Database
   │                                 │                            │
   │ 1. User clicks delete           │                            │
   │ 2. Show confirm dialog          │                            │
   │ 3. User confirms                │                            │
   │ 4. useClientsStore.delete()     │                            │
   │                                 │                            │
   │────DELETE /clients/:id──────────▶│                            │
   │                                 │                            │
   │                                 │ 5. AuthGuard.verify()      │
   │                                 │ 6. Check permissions       │
   │                                 │ 7. Check active records    │
   │                                 │                            │
   │                                 │────SELECT COUNT(*)────────▶│
   │                                 │    FROM appointments       │
   │                                 │    WHERE client_id = ?     │
   │                                 │    AND status = 'active'   │
   │                                 │◀───Count: 0────────────────│
   │                                 │                            │
   │                                 │ 8. Soft delete             │
   │                                 │────UPDATE clients─────────▶│
   │                                 │    SET is_active = false   │
   │                                 │    WHERE id = ?            │
   │                                 │◀───Success─────────────────│
   │                                 │                            │
   │                                 │ 9. Log change              │
   │                                 │────INSERT INTO changes────▶│
   │                                 │◀───Success─────────────────│
   │                                 │                            │
   │◀───Response: 204 No Content─────│                            │
   │                                 │                            │
   │ 10. Remove from store           │                            │
   │ 11. Update UI                   │                            │
   │ 12. Show toast                  │                            │
```

---

## 🎯 Ключові принципи архітектури

### 1. Розділення відповідальності
- **Frontend**: UI, UX, валідація форм, state management
- **Backend**: Бізнес-логіка, валідація даних, безпека, база даних

### 2. Однонаправлений потік даних
```
User Action → Store Action → API Call → Backend → Database
                ↑                                      ↓
                └──────── Response ←──────────────────┘
```

### 3. Type Safety
- Frontend: TypeScript типи для всіх даних
- Backend: TypeScript + DTO validation
- Database: Prisma schema (type-safe ORM)

### 4. Безпека на кожному рівні
- Frontend: Input validation, XSS protection
- Backend: Authentication, Authorization, SQL injection protection
- Database: Encrypted passwords, secure connections

### 5. Масштабованість
- Модульна архітектура
- Кешування (Redis)
- Черги (Bull) для важких задач
- Горизонтальне масштабування

---

**Версія**: 1.0.0  
**Дата**: 03.12.2025
